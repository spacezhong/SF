import React,{Component} from 'react';import ReactSwipe  from 'react-swipe';import './index.less'import Loading from "../../../components/Loading/index";export default class HomeSlider extends Component{    /* componentDidMount(){       getSliders().then(sliders=>{           console.log(sliders);       })     }*/    constructor(){        super();        this.state={index:0}    }    render(){        let swipeOptions={            auto: 3000,            continuous: true,            callback: (index)=>{this.setState({index})},        };        let swipe=(            <ReactSwipe swipeOptions={swipeOptions}>                {                    this.props.sliders.length>0&&this.props.sliders.map((item,index)=>(                        <div className="swiper-ljb" key={index}><img className='swiper-img ljb' src={item} alt=""/></div>                    ))                }            </ReactSwipe>        );        return (            <div className='home-sliders-ljb'>                {this.props.sliders.length>0?swipe:<Loading/>}                <div className='dots'>                    {                        this.props.sliders.map((item,index)=>(                            <span className={'dot '+(index==this.state.index?'active':'')} key={index}></span>                        ))                    }                </div>            </div>        )    }}